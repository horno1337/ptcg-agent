__pycache__/
*.pyc
.pytest_cache/
.venv/
venv/
submission.tar.gz
submission-*.tar.gz
replay*.html
replay*.json
replays/
# built official engine (competition-use-only source lives outside the repo)
engine/
.claude/settings.local.json
tools/checkpoints/
agent/weights.npz

# Completed one-off Dobi-v2 elite-teacher experiment harnesses.  Keep the
# reusable runtime regression test; these artifact-lineage/gate tests depend
# on ignored local checkpoint trees and are obsolete for ongoing development.
/tests/test_dobi_v1_elite_teacher_card_lineage.py
/tests/test_dobi_v1_elite_teacher_card_v1.py
/tests/test_dobi_v1_elite_teacher_card_v1_field.py

# Card/attack database dumped verbatim from the closed competition engine.
# Regenerate locally with `python tools/dump_cards.py` (needs the engine build).
data/cards.json
data/attacks.json

# Private working notes / handoff record.
CLAUDE.md

# ctypes binding + build recipe for the competition engine's private ABI.
tools/cabt.py
tools/build_engine.sh
